<section class="ncs-contact-page">
  <div class="ncs-contact-layout">
    <header class="ncs-page-hero ncs-contact-hero" data-nc-reveal>
      <h1>Get in <em>touch</em></h1>
      <p>We respond to every enquiry within one business day. For trade and project enquiries, visit our B2B page.</p>
      <a class="ncs-btn ncs-btn--ghost" href="{% render 'nc-page-url', handle: settings.page_b2b_handle, view: 'b2b' %}">B2B Enquiry ↗</a>
    </header>

    <div class="ncs-contact-visual" data-nc-reveal aria-hidden="true">
      {% capture contact_image %}{% render 'nc-showcase-image', pool: 'about' %}{% endcapture %}
      <img src="{{ contact_image | strip }}" alt="" width="1200" height="900" loading="lazy">
    </div>

    <div class="ncs-contact-grid">
      <div class="ncs-contact-card" data-nc-reveal>
        <span class="ncs-contact-label">WhatsApp</span>
        {% if settings.whatsapp_url != blank %}
          <a href="{{ settings.whatsapp_url }}">Message us ↗</a>
        {% else %}
          <span class="ncs-contact-value">Available on request</span>
        {% endif %}
        <p class="ncs-contact-note">Fastest response for quick questions and order updates.</p>
      </div>
      <div class="ncs-contact-card" data-nc-reveal>
        <span class="ncs-contact-label">Email</span>
        <a class="ncs-contact-value" href="mailto:{{ settings.contact_email | default: 'hello@nerocasa.com' }}">{{ settings.contact_email | default: 'hello@nerocasa.com' }}</a>
        <p class="ncs-contact-note">For general enquiries, custom pieces and order support.</p>
      </div>
      <div class="ncs-contact-card" data-nc-reveal>
        <span class="ncs-contact-label">Call</span>
        {% if settings.contact_phone != blank %}
          <a class="ncs-contact-value" href="tel:{{ settings.contact_phone }}">{{ settings.contact_phone }}</a>
        {% else %}
          <span class="ncs-contact-value">Available on request</span>
        {% endif %}
        <p class="ncs-contact-note">Studio hours, Monday to Friday.</p>
      </div>
    </div>
  </div>
</section>

{% schema %}{"name":"Contact","presets":[{"name":"Contact"}]}{% endschema %}
